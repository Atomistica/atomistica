subdir('support')
if atomistica_target == 'standalone'
    subdir('standalone/core')
endif
subdir('special')
subdir('potentials')
if atomistica_target == 'standalone'
    subdir('standalone/entrypoint')
endif
